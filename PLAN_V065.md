# Cortex 0.6.5 delivery plan

## Objective and authority

Publication override, 2026-09-13: the user explicitly requested publishing
0.6.5 even unfinished. Publish an identified preview, not a stable release or
main merge. PREVIEW_V065.md is the public status; earlier hold-version
instructions below describe the original stable-release gate and are historical.
Private receipt identifiers are redacted here; complete originals are retained
in the ignored local evidence directory.

Codex is the user interface and operator. ChatGPT is the planning brain.
Cortex supplies bounded, versioned context and turns verified execution reports
into the next planning input. The user authorizes implementation and technical
acceptance by Codex. Stop for a major error or material change in architecture,
scope, access, cost or data exposure. Target: 2026-09-12, Europe/Paris.
The date never changes acceptance criteria. Keep the release version unchanged
until mandatory gates have passed. No invented live evidence or model identity.

## Execution contract

After each stage update this file, V065_TESTS_BENCHMARK.md and primer.md.
Statuses: PASS, FAIL, UNCLEAR; unexecuted stages are UNCLEAR.
Record source revision, exact checks, outputs, limitations and next action.
Use the existing candidate checkout and preserve other contributors' edits.
Do not make Web/TUI redesign part of this release. Do not silently replace
ChatGPT with an API model, browser session or different conversation.

## Current release snapshot — authoritative over historical progress notes

The entries below this snapshot preserve past failures and intermediate states.
Their old "next" or "pending" wording is historical, not the current queue.
The original S0-S9 and T01-T15 requirements remain mandatory; no gate is removed.

| Area | Verified now | Remaining before release |
|---|---|---|
| Native transport | Actual text and authorized Chrome file/image/ZIP pilots | Native attachment capability still absent; integrated attachment/context-request workflow needs complete proof |
| Brain/operator loop | Real normalizer and Luna relay; SIGKILL journal recovery; host reference readback; two real bound chats recall distinct tokens and B continues after A stop | Full project/action isolation, desktop/session loss, launch-to-handle crash window and complete execution-limit proof |
| Context and evidence | Bounded inventory, source freshness, checked decisions, immutable receipts; actual 28-file selection and requested inline content read | Large-context batching and complete requested-attachment workflow coverage |
| Installation | Disposable package, lifecycle/recovery, installed CLI and actual negative-capability model preflight | Fresh macOS user onboarding and complete installed-agent positive workflow |
| External evaluation | Functional organizer, board and worker relay pilots; Freebuff smoke | Frozen paired trials, independent clean contexts and comparative accounting not run |
| Publication | Candidate changes/private evidence retained; full850 backend/resource gate PASS after host-reference change | Final review/privacy, frontend checks, version/README/llms/diagram alignment, commit and authorized publication |

Next safe work after this increment: finish integrated recovery/stop and context
coverage. The user authorized eight synthetic Chrome chats: two isolation and
six benchmark brain conversations. Preserve that bound; do not recycle a
solution-contaminated discussion and call it a fair comparison.
Windows is deferred by the user, not validated by macOS evidence.

## Implementation journal (newest first; retain failures)

2026-09-13 publication: user requests unfinished0.6.5. Metadata and static build
aligned, PREVIEW_V065.md added; private receipt identifiers removed from public
plans after preserving local originals. Fresh build PASS,45 focused PASS.
Full850 run:849 passed/1 stale terminal version expectation failed (192.496s);
expectation corrected, targeted terminal/version21 PASS (4.839s). No fresh
full850 PASS claim after that test-only update. Secret scan of staged diff PASS;
privacy FAIL1 remains the reviewed synthetic redaction fixture. No gate change.
Publish candidate branch and v0.6.5-preview.1 only; main unchanged.

Authorized live isolation bootstrap: two new ChatGPT conversations created in
Chrome, independently readable through Codex native tools. UI model selection
is "Le plus récent", reasoning "Très élevé"; no explicit Astra identity proof.
Different synthetic tokens recalled correctly through two actual Store-bound
native requests. Each send reconciled against two fresh host observations;
cross-conversation prepare refused. Initial stale reads retained, no resend.
Scoped stop blocks A; B follow-up returned B-CONTINUE and was reconciled from
two actual reads. Both synthetic missions then stopped, no unresolved sends.
39 focused journal/crash/handle regression tests PASS in1.540s. Private evidence
in .qa-live-v061/LIVE_ISOLATION.md. Six benchmark chats not created yet.
No production code change, complete project, benchmark or release claim in this increment.

Generated-output whitespace finding triaged: the flagged trailing space is inside
a Next.js Server Action error template literal, also present in the installed
next/dist/client/components/router-reducer/reducers/server-action-reducer.js.
It is string data, not source indentation. No manual bundle trim, Git attribute
exception or compiler patch applied. Full git diff --check still reports it;
the separately scoped check excluding frontend/out passes and is not represented
as a whole-tree PASS. Built output and prior browser evidence remain unchanged.
Release review must retain this distinction; no publication occurred.

Production static export rebuilt successfully using installed Next binary.
Fresh browser suite initially23 PASS/3 FAIL/1 optional skip: all three approval
scenarios omitted pending_approval_action_id, required by the real API and UI.
Fixture now supplies a1 and asserts expected_action_id=a1 in the refusal request;
production refusal protection unchanged. Fresh complete suite26 PASS/1 optional
guide-generation skip in28.4s. Initial traces retained in frontend/test-results;
rerun evidence in a separate private temporary output. Fixture-only latency:
cached usable141ms, ten switches p95/max71ms, not provider/backend timing.
No authenticated conversation or full installation/toolchain claim.

Fresh frontend verification:214 Vitest tests PASS, TypeScript noEmit PASS.
Runtime/config/privacy suite initially35 PASS/1 FAIL: toolbar detail hover/focus
used undefined --text. Replaced that reference with existing theme --ink, no new
palette/token or gate change. Same36 tests now PASS (4.721s); oxlint PASS.
Used installed binaries, not npm11.18.0; toolchain installation still unverified.
No new screenshot, rebuilt export or live authenticated browser claim in this
increment. Public privacy fixture finding and clean-chat authorization remain open.

Public-tree privacy diagnostic initially FAIL5 using configured fingerprints,
URL allowlist and CI markers. Removed personal-name references from planning
documents and a contract test; neutral synthetic workspace preserves the same
no-disclosure assertion.35 loop/context tests PASS in2.765s. Fresh full scanner
now FAIL1: synthetic absolute-path fixture in tests/test_supervisor_context.py:26.
Scanner/policy unchanged; no privacy PASS or publication claim. The remaining
fixture signal needs explicit treatment preserving redaction coverage, not a
blanket allowlist or encoded path. Owner-specific marker coverage remains separate
from the configured CI-marker diagnostic. No new ChatGPT conversations created.

Fresh complete backend after host-reference increment:850 tests PASS in177.137s,
strict ResourceWarning/unraisable GC tracking reports zero unclosed resources;
overall exit0 in177.727s. Base HEAD7b740095f9cb6a1d68cc131fb8371cb30281056a plus
uncommitted changes. Tested store.py SHA256
d52c8c0d506afb3cae3fa52131829abf6775ae184ae6f2e197f45d324ba333ea;
native_journal.py cf12b6c6753d6d5537ea88ce14d1051d20b1d9be36a9df7c0ba669f549a94022.
Current snapshot refreshed without relaxing gates. Asked user for eight clean
Chrome ChatGPT conversations (two isolation, six paired-task brain trials);
no new conversation created pending reply. Fresh macOS user validation remains
distinct from disposable installation. No release/version change or publication.

Native host-reference gap corrected: guide required recovery but journal had no
session reference. Five missing-method test errors before implementation; immutable
scoped attach_handle now uses existing transport events and status exposes it.
Wrong mission/kind, replacement, invalid strings and cross-action reuse refused;
identical retry allowed, late pending reference after stop retained.68 targeted
tests PASS in6.041s, skill validator PASS. Real host session persisted, read via
fresh CLI, polled while running then observed output/exit0 and settled without
relaunch. Runtime database retained in private local evidence, not a public path.
This tests journal reopen, not desktop restart or launch-before-attach recovery.
Full843 baseline predates this production increment; full rerun remains needed.

Two new real local crash/isolation regression tests PASS in0.354s;81 related
checks PASS in2.699s. Child durably reserves a bound outbound, parent observes
readiness then sends actual SIGKILL; fresh CLI sees pending and rejects retry.
Independent project B can reserve; scoped stop A preserves both pending records
and leaves B IDLE. Same relative file name with different project bytes and wrong
conversation binding are rejected before reservation. No production changes
needed; existing behavior characterized, no manufactured red/fix claim.
tests/test_native_crash_isolation.py. This is journal-process failure, NOT
power-loss, a surviving external executor, or two live ChatGPT conversations.

Real inventory-selection pilot PASS:28 eligible synthetic files/24 distractors,
one excluded payload. Brain requested relevant format document from metadata;
actual bounded read then inline transfer; final reply returned two exact random
labels in correct roles plus duplicate behavior. Both sends confirmed with two
actual host reads, no resend during read lag. Original labels absent from initial
message, excluded payload absent from both sends. Operator verified and persisted
COMPLETED, release_eligible=false. Private .qa-live-v061/INVENTORY_SELECTION.md.
No large-project, independent benchmark, new attachment or worker claim.

Installed operator guide now routes metadata inventory, explicit exclusions,
bounded file selection and native completion after concrete semantic review.
Fresh post-inventory backend:843 tests PASS in175.865s; strict GC/resource probe
zero unclosed resources, overall exit0 (176.406s). Targeted92 PASS in6.063s;
skill validator and git diff --check PASS. No new live model trial or publication
in this increment. Next: actual inventory-driven context selection with ChatGPT.

Full pre-inventory baseline:837 tests PASS in177.688s; strict resource probe
reports zero unclosed resources, overall exit0. Inventory then implemented in
existing artifact helper, no new transport/service: explicit relative metadata,
excluded entries and incomplete-scan reporting, no file payload reads.4 initial
missing-operation FAILs;92 targeted tests PASS in5.781s, including real installed
Python-I invocation. Actual candidate scan observes749 entries/655 files/14
exclusions, no issues, eligible scan complete but full-project coverage false.
No index or source sent to a model in this step. Full837 result predates this
increment. Next: connect inventory selection to the installed operator guide
and validate a real large-context selection/request loop without silent omission.

Full backend837 tests PASS in175.754s, but strengthened ResourceWarning/GC gate
FAIL: one unclosed SQLite connection. Isolated allocation tracing proves the
CrossRouteLeakRegressionTest fixture created it and failed to close it before
restoring globals. Teardown now cancels/awaits its own tasks and closes its Store.
All8 sharing tests PASS with zero unclosed resources under the strict probe.
The full post-fix warning-free verdict is not yet established; rerun next.

Next context-builder increment, grounded in current implementation: artifacts
CLI handles explicit --file preparation and freshness verification but has no
inventory operation. Add a read-only --inventory mode to the existing helper,
not a new service. Return bounded relative file metadata plus explicit exclusions
and incomplete-scan status; never follow links, read file payloads, or transmit.
The operator reviews that index before selecting batches under existing artifact
limits. Freeze selected bytes only during preparation, not by pretending a
metadata scan grants approval or delivers files. Tests must cover ordinary tree,
hidden/private entries, links, invalid names, scan limits, missing root and the
installed isolated-Python CLI. A incomplete scan must never report full coverage.
Full regression uses explicit ResourceWarning/unraisable collection; its
functional test count and resource-cleanliness verdict are reported separately.

Inherited-pipe timeout defect reproduced: leader exits, SIGTERM-ignoring child
retains pipes and run_process hangs past its deadline. Cleanup now finishes
the owned group even after leader exit before draining output. Actual FAIL then
PASS;70 executor/native-journal/action/recovery tests PASS in5.296s. A separate
strict ResourceWarning/GC probe identified our reopened finalization test's
unclosed second Store; registered its cleanup, probe now reports zero unclosed
resources. This fixes that leak only, not every earlier warning in the suite.
Native host watchdog/hard-crash recovery and full regression remain separate.

Executor cancellation defect reproduced with real POSIX processes: cancelling
run_process left its parent and child alive. Added owned-group SIGKILL/reaping
on CancelledError; repeated cancellation cannot abandon that cleanup task.
Original FAIL retained in test-first run; parent plus SIGTERM-ignoring descendant
now stop, cancellation propagates.37 executor/native-action/recovery tests PASS
in3.323s. No deletion of project files and no fabricated success receipt.
This fixes ToolExecutor, not every native host process owner. Abrupt coordinator
death, cancellation during startup/timeout cleanup and separate native host
handle recovery remain distinct gaps; T11 is not promoted wholesale.
Next targeted check: timeout with a leader already exited but a child holding
stdout open; verify cleanup completes rather than waiting on inherited pipes.

Installed finalization test caught status omitting the persisted mission state.
Fixed status to return state and iteration alongside pending work. Real isolated
installed processes now complete, reopen, expose COMPLETED and refuse new sends.
The same test retains rejection of tampered proof after completion. This is
synthetic conversation input, not another provider-backed trial.
Fresh86 targeted tests PASS in5.795s. Separately installed current bundle reads
the actual completed worker-relay database outside checkout: COMPLETED, iteration3,
no pending messages/actions, exit0. No new brain request or executor run.
Full835 regression predates this status-response fix; SQLite warning remains.

Guarded native finalization implemented: current COMPLETE context, full passing
retained evidence and explicit per-criterion operator review checked under one
SQLite writer transaction, then decision/validation/terminal state persisted.
Missing review, failed criterion, stale proof, cancellation and replay refused;
read-only checks remain non-finalizing and no human approval/release is inferred.
Initial3 missing-capability FAILs then86 targeted PASS in5.609s. Full backend
835 tests PASS in176.885s, exit0; existing SQLite ResourceWarning observed. Real worker-relay pilot
now stored COMPLETED after actual review, validation native-final:[private receipt ID retained locally].
This ends that task only, not S7-S9 or Cortex0.6.5. Source point-in-time and
caller-supplied observations/reviews remain explicit limitations.

Worker relay repair: actual second worker changed only README's interpreter,
exit0 in28.969s. Exact documented command independently runs15 tests PASS;
source/tests/brief/prior proof unchanged. Initial delivery FAIL retained, final
semantic acceptance PASS for this task. Operator cache false assumption caught
and reproduced (-I ignores environment bytecode flag; use explicit -B); cache
retained and disclosed. Final report reconciled; stable COMPLETE proposal matches
current revision/iteration3. No pending action/message; task accepted by operator.
The read-only helper intentionally does not mark the stored mission completed.
Next code gate: persist native finalization only after checked current COMPLETE
proposal, full retained criterion evidence and explicit operator semantic review;
pending/stale/failed evidence must refuse it. Read-only evidence checks alone
must continue to return completion_verified=false.

Real worker relay now executed: actual ChatGPT bound run_process decision fed
unchanged to a separate requested Luna worker. Three files authored, process
exit0 in64.528s; independent functional checks and15 worker tests PASS. Semantic
delivery FAIL: README still recommends the unavailable python alias despite
worker recovering with python3. Original files/results preserved; immutable
receipt records failure and no operator repair. Same brain received exact
source/tests/report for a README-only worker repair; awaiting reply. This is
functional relay evidence, not a paired benchmark or full autonomous release.

Real installed operator preflight: Codex CLI0.144.1 requested Luna read the
installed skill/reference and stopped on absent native conversation tools.
Read-only synthetic project unchanged, exit0,33.667s. PASS for honest preflight
only; --ignore-user-config means this is not a universal CLI capability verdict.
Next: capable desktop coordinator relays a bounded actual brain job to an
external executor, gathers independently verified results and returns them to
the same ChatGPT thread. Worker transport and full autonomous loop remain open.

Live bound normalizer pilot (2026-09-12): real stable brain reply checked against
the frozen source revision, exact action reserved before apply_patch, unchanged
source passes all four independent criteria. Original proof retained; fresh
read-only verification exits0. Immutable action receipt now includes proof
hashes; acceptance evidence integrity and reported criteria PASS. This is a
small functional protocol pilot, not a benchmark or release completion. Final
review report reconciled; stable brain COMPLETE proposal matches revision and
iteration2. Zero pending actions/messages; automatic completion remains absent.
Fresh80 targeted tests PASS in4.488s; full backend829 PASS in173.455s, exit0.
The existing SQLite ResourceWarning remains; no frontend rerun or release claim.
Installed instructions now prioritize frozen acceptance, prepare_context and
prepare_context_action; legacy operations are explicitly not a fallback.
New real isolated subprocess test installs the package outside the checkout,
reserves the fixture decision, creates exact result bytes, checks retained proof,
and refuses unchecked actions, replay and changed proof bytes. Conversation
observations are fixtures, not a live installed-agent behavior claim.
81 targeted tests PASS in5.059s; skill validator and git diff --check PASS.
Prior829 full backend run predates this added test. Next: verify an actual
operator follows the installed instructions, not only that helpers support them.

Acceptance evidence map: each frozen criterion must reference a completed native
record and proof manifest retained in its immutable receipt. Coverage, source
hashes and failure consistency checked; reported verdict stays distinct from
evidence integrity and completion. Three initial FAILs then80 targeted PASS.
Semantic coverage/final review remain required; full826 baseline predates this.

Acceptance baseline: define_acceptance freezes unique nonempty criteria before
work; changes/reductions and late definition refused, identical repeats safe.
Stored contract is included in context requests, never labelled completed or
approved. Two missing-capability FAILs then77 targeted checks PASS. Final
evidence evaluation remains open. Fresh full backend826 PASS in172.060s, exit0;
SQLite warning remains. Skill validator and git diff --check PASS.

Bound-action increment: context missions now reject unchecked preparation.
prepare_context_action derives exact action from checked reply; transaction
rechecks source/request, records decision+reservation and advances iteration.
COMPLETE cannot reserve effects. Two pre-fix FAILs then75 targeted tests PASS.
CLI path executed a real synthetic print process and recorded its actual exit;
brain observations remain synthetic. Host authority and final acceptance are
separate. Legacy context-free missions remain compatible, not newly supervised.

Reply context check implemented: latest confirmed request, immediate stable
assistant receipt from two supplied observations, exact revision/current sources,
existing cortex.v1 schema. Initial missing-checker tests FAIL then targeted PASS.
COMPLETE remains a proposal: no state transition/action or permission emitted.
Caller observation provenance, delivery-content and mandatory action/final gates
remain open. Tests are synthetic observations with real filesystem/SQLite.

Revision-bound requests: prepare_context verifies selected sources under the
stored mission workspace and retains the same selection hash in wire/checkpoint.
Unknown manifest extensions and claimed delivery fields are not forwarded.
Missing-capability regression FAIL then61 targeted tests PASS, including actual
CLI reservation-before-return and duplicate refusal. Request binding only:
response validation and mandatory action binding remain open; full812 is older.

Source freshness increment: verify_sources/--verify-manifest compare bounded
selected current bytes with retained item metadata. Same-size/mtime changes
are detected; malformed manifests/path escapes/symlinks fail. No publication.
Initial missing-capability tests FAIL then37 targeted tests PASS; installed
Python-I verification also exercised. Point-in-time check only; no authenticity,
delivery or automatic decision-binding claim. Full812 baseline predates this.

Native round guard: persisted native send attempts now consume max_iterations
(default25), including aborted reservations. Two regressions FAIL before fix;
51 targeted checks PASS afterward. Reopen/changed payload cannot reset budget;
legacy web loop semantics unchanged. This is an outbound cap, not a local action
counter or validated decision-cycle protocol. Fresh full backend812 PASS in
171.804s, exit0, covering the stop/race/deadline/attempt changes. SQLite warning
remains. Official skill validator and git diff --check PASS.

Native stop increment: explicit scoped stop CLI now persists CANCELLED, preserves
pending receipts and is idempotent. Other missions are unchanged; late receipts
remain recordable. Native send reservation rechecks state/deadline under writer
lock. Regression FAILs observed before fix;49 targeted tests PASS including real
CLI processes. External process termination and iteration limits are separate
open gates. Latest full808 baseline predates this and the outbound-race change.

Outbound contention correction: instrumented real-SQLite test first reproduced
two simultaneous different-payload reservations. One immediate transaction now
serializes read/check/write; 48 targeted tests PASS. Four real subprocesses give
one prepared/three refused and one durable event. Not remote exactly-once proof.
Latest full808 run predates this increment; no fresh809 full-suite claim.

Native deadline increment: expired missions now refuse new native message and
action reservations using persisted start/duration; late receipts still work.
Three regression FAILs observed before fix; 37 targeted tests PASS afterward.
Two real CLI invocations return exit2 and leave zero reservations. Not a process
watchdog or complete T11 proof; iteration/action-count enforcement remains open.
Fresh full backend808 PASS in171.329s, exit0; SQLite warning remains.
Official skill validator PASS. Native T11 remains incomplete beyond deadlines.

Benchmark preparation increment: organizer contract/evaluator frozen in ignored
QA evidence, seeds6501/6502/6503, counterbalanced arm order. Four verifier controls
PASS including real positive/negative subprocesses. No paired trial started.
The existing brain pilot conversation is contaminated with prior solutions;
a clean benchmark context must be established, not claimed by prompt reset.

| Stage | Objective and work | Deliverable / acceptance | Status |
|---|---|---|---|
| S0 Grounding | Check Git, current protocol, tools and latest supervisor conversation | Baseline revision and fresh read; distinguish attachment read from send | PASS |
| S1 Real transport | Send a unique text marker; read actual response; test a new file, image and ZIP on the same conversation | Text, Chrome document/image/ZIP read PASS on this host; native desktop attachment still unavailable; autonomous integration is a later gate | PASS |
| S2 Context builder | Inventory tracked project code/docs, select required files, freeze bytes, hash and size each item, report exclusions, index large context in lots | Deterministic manifest; no external paths, secrets or silent truncation; changed bytes invalidate approval | UNCLEAR |
| S3 Brain/operator contract | Bootstrap roles, original request, constraints, evidence, missing context; validate decisions and bounded task dependencies | Reuse REQUEST_CONTEXT, EXECUTE, COMPLETE, BLOCKED with mission/turn/action IDs; planning/review are activities | UNCLEAR |
| S4 Durable loop | Persist conversation binding, outstanding decision, context versions, execution receipts and recovery checkpoint | Restart resumes without duplicate send/write; uncertain delivery stops before retry; limits and stop work | UNCLEAR |
| S5 Agent execution | Give each worker task, prerequisites, file ownership, allowed actions and acceptance; return diffs, failures and proof | Independent tasks only in parallel; supervisor sees failures and directs correction; existing authorization retained | UNCLEAR |
| S6 Agent installation | README entry point, agent install instructions, scoped setup, capabilities probe, uninstall and update | Fresh disposable setup supports bootstrap and reports missing prerequisites; no interactive terminal required of user | UNCLEAR |
| S7 Acceptance | Run functional battery and two external projects using live supervisor | Mandatory cases pass with actual outputs; synthetic tests kept separate | UNCLEAR |
| S8 Benchmark | Freeze tasks and criteria before trials, same executor baseline versus ChatGPT + executor | Raw per-run evidence, success/latency/interventions and uncertainty; no equivalence marketing | UNCLEAR |
| S9 Release | Review code/docs, privacy and regression checks; update version metadata, README, llms.txt and diagram | Consistent 0.6.5 metadata only after mandatory gates; Git diff reviewed, committed and publication state verified | UNCLEAR |

## Dependencies and implementation boundaries

S0 -> S1 -> S2/S3 -> S4 -> S5/S6 -> S7 -> S8 -> S9.
S2/S3 specifications and benchmark fixtures can be prepared while S1 runs,
but an unsupported desktop attachment capability is an architecture decision,
not a reason to mark S1 successful with text pasted in its place.

Reuse orchestration/context.py for bounded manifests and redaction;
orchestration/protocol.py, state.py and loop.py for decision validation;
existing executor and policy for actions. Inspect actual signatures before edits.
Keep transport capability detection separate from planning. Application-native
Codex tools are host capabilities, not automatically callable Python functions
or portable capabilities available to every installation.

## Proposed envelopes (design, not implementation proof)

Context: protocol, mission_id, revision, original_request, goal, acceptance,
constraints, project_inventory, selected_items, excluded_items, capabilities,
missing_information. Item: id, relative_path, MIME, bytes, sha256, reason,
delivery_state, actual_message_reference.

Decision: protocol, mission_id, turn_id, based_on_revision, kind, rationale,
required_context_ids, tasks[{id, dependencies, files, instruction, acceptance}],
stop_reason. Report: action_id, decision_reference, actual_changes, checks,
exit_codes, evidence_references, failures, unresolved_questions.

Do not let a model-authored COMPLETE replace local acceptance checks.
Do not run arbitrary shell text solely because it appears in a decision.
An image needs image delivery; a path, OCR or textual summary is not equivalent.

## Current evidence

- Baseline 7b74009, clean worktree before this plan; declared version 0.6.1.
- Existing supervisor conversation was read through the desktop tool.
- Historical file/ZIP attachments are returned by that reader.
- Desktop send-message tool exposes prompt text but no attachment argument.
- New CORTEX-065-BRAIN-ACK request returned an actual assistant response in
  the same conversation. See V065_TESTS_BENCHMARK.md for references.
- No 0.6.5 live mission or benchmark has yet passed.

## Brain review and operator decisions

The live supervisor recommends reusing the existing loop and keeping four
decision states (EXECUTE, REQUEST_CONTEXT, COMPLETE, BLOCKED), starting with
one action per iteration. Adopt this simpler design instead of adding PLAN
and REVIEW runtime states. Planning/review remain activities, not new states.
It requests runner.py, loop.py, protocol.py, state.py, context.py, executor
tools/policy, mission lifecycle, persistence and relevant tests. It does not
claim to have received these files for this mission.

Do not adopt its proposed artifact fallback as equivalent to image/file/ZIP
delivery: actual attachments remain a user requirement. Do not adopt its
weighted overall benchmark score; retain per-criterion evidence and paired
outcomes. Ten trials per arm on large third-party repositories are a later
benchmark expansion; current three paired trials are preliminary and time-boxed.

## Major blocker — execution paused on 2026-09-12

Update: the user subsequently authorized the Chrome attachment channel ("go
continue"). Work resumed. The earlier desktop restriction below remains a
capability limit, not a current request for the same authorization again.

The desktop send tool only exposes text. A real attempt to inspect the Codex
app with the UI tool returned: "Computer Use is not allowed to use the app
'com.openai.codex' for safety reasons." Do not retry through another automation
mechanism to bypass that restriction. No new attachment was sent or inspected.

Decision required: whether to permit a separately supported ChatGPT browser
attachment channel while retaining Codex as the user interface, or retain the
desktop-only constraint and wait for a supported native attachment capability.
Browser delivery is not verified by this stage and is not an implicit fallback.
The current request requires stopping for this material architecture change.
Version remains 0.6.1; no release, commit or push is claimed for this work.

## Resumed execution

- T02 PASS through Chrome: the synthetic document was attached and ChatGPT
  returned its unseen verification value. Desktop read_thread independently
  returned that same user/assistant exchange (IDs in the test ledger).
- A click timed out after submission, but reconciliation found the real reply;
  no resend was performed. Preserve this case in delivery recovery tests.
- Image/ZIP upload remains unvalidated. Concurrent foreground changes to
  unrelated tabs make native interaction unreliable; pause native input until
  Chrome is free. Tab-scoped control also had intermittent CDP timeouts.
- Implemented orchestration/artifacts.py: explicit bounded selection, no-follow
  reads, immutable bytes, archive checks, deterministic packaging and agent CLI.
  Twenty-two targeted tests and eight existing context tests pass. S2 remains
  partial: inventory and full transmission integration are not complete.
- HARNESS_V065.md provides the operator procedure; documentation is not runtime
  integration proof. ChatGPT received the new module as exact source text for
  review, explicitly not as an attachment. Review identified incomplete final
  writes, detectable concurrent source changes and unnormalized root errors;
  corrections are implemented and covered locally. Authorized root binding
  remains an integration requirement, not a claim made by this utility.
- New continuity concern: after sending via a stale browser view, the desktop
  history's latest path did not include the earlier brain ACK. Same URL alone
  is insufficient: require last-message/turn-marker reconciliation before send.

## Continuation evidence

- Browser reload restored the missing review, then a second reload after the
  next desktop exchange showed CORTEX-065-REVIEW-FOLLOWUP and its response.
  This demonstrates stale rendering; branch divergence is a risk, not proven.
- The desktop follow-up send returned before read_thread exposed it. Repeated
  reads eventually found request [private receipt ID retained locally] and reply
  [private receipt ID retained locally]; no duplicate was sent.
- Supervisor recommends an expected-head check, an explicit uncertain-send
  state and reconciliation before retries. Do not claim server-side atomic
  compare-and-swap: the available tools do not expose such a guarantee.
- T03/T04 chooser attempt timed out before file selection; native Chrome was
  on another workflow. Neither fixture was submitted. Do not steal native
  foreground control from concurrent work.
- Two additional artifact regressions were reproduced before fixing them:
  mutable bytearray payloads and control/format characters in archive paths.
  Both now fail closed; 20 artifact tests PASS.
- CLI r2 created a complete exclusive 15,751-byte archive, SHA-256
  94678e042ebb95e221195aa076965d8d95c5be4136ddcc84742b44602440b302.
  It is prepared, not transmitted. No live mission or benchmark result yet.
- Fresh full backend run: 766 tests in 165.966 seconds, exit 0. An unclosed
  SQLite connection warning remains observed; frontend was not rerun.
  S2-S9 are not promoted by this regression result alone.
- S2 integration: `--goal` now returns a validated desktop-supervisor.v1 packet
  alongside the archive manifest. Item hashes/sizes derive from the same frozen
  bytes; no second filesystem read and no delivered flag. Two real subprocess
  tests prove CLI-to-archive-to-context consistency and rejection of an oversized
  objective before creating a final archive. Both failed before implementation,
  then passed. Focused result is now 22 artifacts + 8 supervisor-context tests;
  the 766-test full run predates these two additions.

## S4 durable outbound increment

- Runner now persists MESSAGE_SEND_STARTED in the existing SQLite event store
  before calling the transport. Mission-scoped payload hashes prevent automatic
  replay after interruption. Persistence failure prevents the transport call.
- MESSAGE_DELIVERED includes send_id, payload hash, UTF-8 byte count and the
  returned user message ID when exposed. No reservation is a delivery receipt.
- Existing definitive pre-delivery error codes produce an append-only
  MESSAGE_SEND_ABORTED event. Only a last aborted attempt permits retry with a
  fresh send_id. Unresolved sends also block a different payload in that mission.
- Six focused recovery tests PASS (simulated transport, real SQLite/reopen).
  Seven existing runner tests PASS. A full 771-test run passed in 166.446 s
  before the last three recovery cases/abort handling were added.
- Brain review [private receipt ID retained locally], response
  [private receipt ID retained locally] recommended append-only abort evidence.
  This is design review, not a real transport validation.
- S4 remains partial: automatic observed-history reconciliation, expected-head
  enforcement and host-native Codex tool integration still need implementation.
  Single Store/writer is the existing runtime contract; no cross-process lease
  or server-side atomic send guarantee is claimed by this increment.

## S1 completed on the authorized Chrome channel

- T03/T04 PASS: request [private receipt ID retained locally] carries one
  image and one ZIP. Response [private receipt ID retained locally] correctly
  identifies red circle upper-left / blue square lower-right, both ZIP paths
  and the unseen value violet-comet-4826. Observed in Chrome and desktop reader.
- Browser chooser injection selected files but upload spinners persisted.
  Removed only these synthetic attachments from the unsent draft, then used
  the supported native macOS picker. Native upload completed; submitted once.
  This is a documented channel comparison, not bypassing a security refusal.
- Exact four-file production review archive (runner, store, adapter, missions)
  generated by the artifact CLI and sent to the same supervisor conversation.
  SHA256 9a2db88a09cd04e1addf57834b24901248bd289c49dbcd130c01ba969b270365,
  195488 bytes. Marker CORTEX-065-RECONCILIATION-SOURCES, user message
  [private receipt ID retained locally]. Review completed as assistant
  [private receipt ID retained locally]. Do not resend based on desktop idle metadata.
- Codex remains the user-facing operator; Chrome remains an explicitly
  authorized attachment channel. Passing these host-operated transfers does
  not establish the autonomous runtime, model identity, clean install or benchmark.

## S4 reconciliation increment

- Implemented the supervisor's bounded proposal: persist a conversation-head
  checkpoint, compare it before sending, and append a UUID marker to technical
  mission messages only. Ordinary chat messages remain unchanged.
- Resume now reconciles an unresolved attempt before consuming a decision or
  sending anything. A unique stable user receipt after a known anchor produces
  an append-only MESSAGE_DELIVERED event. Missing anchor, wrong conversation,
  duplicate marker, unstable receipt or fallback message ID refuses recovery.
- No server-side atomic send, malicious-origin authentication, truncated-history
  recovery or automatic first-new-conversation crash recovery is claimed.
- Focused checks: 10 runner/adapter, 8 SQLite recovery and 15 mission API tests
  PASS. These are fixture tests, not live ChatGPT mission acceptance.
- Full backend rerun PASS: 779 tests, 167.522 s, exit 0; the existing unclosed
  SQLite ResourceWarning remains. Host-native automatic tool orchestration,
  remaining external missions, installation and benchmark are still open.

## External organizer pilot 01 — real brain/code execution

- Desktop request [private receipt ID retained locally], marker
  CORTEX-065-EXTERNAL-ORGANIZER-PILOT-01; implementation response
  [private receipt ID retained locally]. Python block applied unchanged.
- Code SHA256 84d0906ec625f6eb13ad232466b81629b6a3e3c8cea49eafc4552512ea64142c.
  Independent evaluator frozen before receipt, SHA256
  d4eab21f30d51543b5ead11c2450ffc9220164127844125c35bcc5115a3af78c.
- Real subprocess pilot passes 24-file coverage, original/copy hashes, extension
  groups, dry-run, idempotence and tested refusal cases. The host Codex agent
  operates native tools; the Cortex web server was not listening on port 8420.
  This is not proof of the autonomous web runtime or a paired benchmark.
- Overall pilot remains UNCLEAR: the README reader truncates at 20000 chars;
  an operator usage note satisfies only the evaluator's weak file-existence
  check, not lossless brain document delivery. Static inspection also finds
  replacement of a different existing manifest and ancestor-symlink gaps.
- Exact findings sent back to the same brain with marker
  CORTEX-065-EXTERNAL-ORGANIZER-PILOT-01-REPORT; correction pending. No silent
  evaluator repair or safety-gate promotion. Raw local pilot is ignored under
  .qa-live-v061/v065-organizer-pilot; synthetic outputs retained in temporary
  storage, with the evidence reference in its PILOT.md.

### Pilot correction, preserved as a separate candidate

- Brain response [private receipt ID retained locally] supplies six targeted
  function additions/replacements plus a complete short README. Applied exactly
  to organizer_corrected.py; original organizer.py and evaluator unchanged.
- Corrected SHA256: 1ed4c39b895b43d452965cfe0c95a652b91f8bf0cdafab4df6af11007b4b81e2.
- Default macOS temporary-path run FAIL: SOURCE contains the system symlink
  /var. Kept as evidence. Explicit physical-root diagnostic under /private/tmp
  passes the original 11 functional criteria. This is not universal path support.
- Four additional checks PASS: conflicting manifest produces no writes;
  source symlink and destination ancestor symlink refused; failed-hash copy
  retains its output. Complete README received as README_SUPERVISOR.md.
- Functional pilot succeeds on the explicit physical-root scope after brain
  correction. Overall release and comparative benchmark remain unproven.
  Sent findings back to the same brain, asking next for the installable native
  harness gap. Do not silently resolve links or promote fixture runtime gates.

## S6 native instruction bundle increment

- Added cortex-supervisor/SKILL.md and scripts/install-supervisor.py.
  Instruction-only bundle: skill plus current HARNESS_V065.md reference.
  Dry-run hashes frozen source bytes and exact target. Apply checks the plan
  hash, creates a new target exclusively and verifies installed bytes.
- Three disposable subprocess tests PASS: plan/apply/no overwrite, wrong-hash
  refusal and ancestor-link refusal. No global skills directory was changed.
- SUPERVISOR_INSTALL.md documents agent-operated setup and post-install live
  capability/discovery gates. The installer does not install an executor or
  claim to expose native host tools to Python.
- Official skill validator unavailable in both tested Python interpreters:
  missing yaml module. Not counted as a pass. Global discovery, lifecycle,
  unattended native loop, external task B and paired benchmark remain open.

### Native skill validation and discovery

- Official quick_validate.py PASS using PyYAML already present in the local
  uv cache, exposed only to the validation subprocess. No dependency or global
  configuration installation. Earlier missing-yaml attempts remain historical.
- Real installer + `codex debug prompt-input` in isolated CODEX_HOME PASS.
  The prompt includes the installed skill description and its actual installed
  path via the root-alias map. No model/provider invocation was performed.
- Initial discovery probe falsely required a literal absolute path. Inspected
  real output, then corrected the probe to verify alias AND matching root.
  Both results retained. The skill/installer were not altered to satisfy it.
- Discovery is not behavioral execution, native desktop tool access, or a clean
  account lifecycle test. Next priority remains a durable native run record,
  then real skill-led acceptance and comparative trials.

## Native journal integration increment

- Added orchestration/native_journal.py: existing Store reservations, bound
  conversation checks, exact payload receipt validation and bounded JSON CLI.
  No desktop tools are emulated or automatically called. Observation provenance
  remains explicitly host_observation_supplied, not authenticated by this code.
- Four native journal tests PASS, including actual CLI subprocess/reopen and
  no duplicate reservation. Eight outbound recovery tests and 32 store-related
  tests PASS. A mistyped suite pattern selected zero tests and was corrected;
  the zero-test result is not evidence.
- Found and corrected an integration defect: Store startup recovery paused a
  running mission when used by a short-lived CLI client. Recovery remains the
  default for runtime owners; only the journal client disables startup recovery.
- Full backend rerun started. Native mission bootstrap, host-tool integration,
  real journal-backed mission and remaining release gates are still incomplete.

### Native bootstrap and live text journal

- Six native tests PASS including stopped/paused refusal and exclusive mission
  initialization. Prior full run: 786 PASS, 168.397 s; predates the last two
  native cases. SQLite ResourceWarning remains.
- Live prepare/send/reopen/refused second preparation/two reads/confirm PASS.
  Attempt [private receipt ID retained locally]; user message
  [private receipt ID retained locally]; assistant
  [private receipt ID retained locally] returned CORTEX-NATIVE-ACK-7319.
- Provenance is host_observation_supplied. Real operator-assisted text path,
  not autonomous host integration or external execution recovery. Private DB
  retained under .qa-live-v061/v065-native-live-01.db.

## Installed native journal bundle

- Installer now includes the stdlib journal entrypoint and its exact existing
  package dependencies in the reviewed hash plan. Scope explicitly changes to
  instructions-and-local-journal; no global installation or permission change.
- Installed skill calls this helper directly, without the original checkout.
  Integration test runs Python -I from another directory and verifies actual
  init/status JSON. Three installer and six native tests PASS; official skill
  validator PASS. No additional full-suite count is claimed.
- Installed artifact preparation is now covered by an isolated subprocess
  integration test; action execution recovery, live skill-led workflow,
  lifecycle and comparative benchmark remain incomplete.

## Installed artifact helper verification

- Added supervisor-artifacts.py and exact existing artifacts/context modules
  to the install hash plan. No third-party dependencies or global installation.
- Test first failed because the installed entrypoint was missing; after bundling,
  Python -I outside the checkout creates a real ZIP with a context packet.
  Assertions check payload bytes, item/archive SHA-256 and non-delivered state.
- 39 targeted installer/artifact/context/native-journal tests PASS; official
  skill validator PASS and git diff --check PASS. Not a new full-suite result.
- Archive preparation is not attachment delivery or autonomous execution.

## Native action recovery — initial implementation

- Existing tool_executions now supports serialized native reservations and
  immutable supplied receipts. No new database schema. Journal CLI exposes
  action_prepare/action_confirm and pending_actions in status.
- Reservations reject paused/terminal missions, any unresolved action and
  reused action identities. Receipts require an actual integer exit and evidence.
- 45 targeted tests PASS, including a real subprocess creating one file then
  os._exit(17) before recording its result. Pending intent survives; replacement
  execution is refused; actual exit/file observation settles the failed attempt.
- This does not prove external tool atomicity, lost-handle reconciliation,
  concurrent cancellation safety or the full installed brain-led workflow.
  These remain open, as do lifecycle, task B and paired benchmarks.

## Installed action workflow and full regression

- Installed Python -I journal tested outside checkout: prepare, real subprocess
  file creation, pending status, actual exit/hash confirmation, settled status.
  Reusing the action is rejected both before and after its receipt.
- Seven focused installer/action tests PASS. Official skill validator PASS.
- Fresh full backend suite: 792 tests PASS, 173.061 seconds, exit 0, command
  PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest discover -s tests -q.
  Existing SQLite ResourceWarning remains; no clean-warning claim.
- This does not complete the live brain-led installed workflow, update/removal,
  lost-handle/cancellation gates, project B or paired comparative benchmark.

## Live installed supervisor pilot

- Installed helpers used with the real bound ChatGPT conversation. Exact request
  confirmed on two fresh reads after stale native history required an authorized
  empty-composer Chrome reload. Response code applied unchanged and evaluated.
- Six functional checks PASS; two evaluator checks FAIL due to a stdout-only
  error expectation absent from the brief (code provides valid JSON on stderr).
  Initial failed run retained and reported to brain; no silent evaluator fix.
- Code exceeds the requested line limit. Follow-up delivery confirmation pending.
- Storage rejected manual temporary-root edits; source explicitly placed in
  authorized QA scope with a separate bound evaluation record. No config change.
- apply_patch does not expose an integer exit: current native receipt contract
  cannot represent its completion truthfully. This remains a concrete integration
  gap; only process execution is recorded by the action helper in this pilot.
- Private evidence and exact message/code hashes: .qa-live-v061/INSTALLED_LIVE.md.
  No paired benchmark, small-model or full release claim.

## Non-process host receipts

- Added tool_prepare/tool_confirm; reservation fixes process versus host_tool
  receipt kind. Actual tool result and verification stored with exit_code=null.
  Missing/unknown outcomes remain pending; receipt kind cannot change later.
- 46 targeted tests PASS, including installed CLI schema round-trip and rejection
  of switching kinds, invented exit, empty verification or unknown outcome.
- Actual apply_patch probe reserved first, returned {}, then file content/SHA
  read and recorded; no fictitious exit0. This proves the local host receipt
  path, not authentication of caller-supplied evidence or full mission success.
- Prior live brain follow-up confirmed by two fresh native reads, user message
  [private receipt ID retained locally]. Assistant [private receipt ID retained locally]
  confirms the evaluator mismatch and requests line-limit/path handling fixes.

## Live inventory pilot correction completed

- Applied the brain's proposed fixes in a separate file, preserving original
  bytes and frozen evaluator. Operator formatting changes explicitly labelled.
- Corrected candidate: 148 lines; 8/8 frozen checks PASS. Additional lexical
  parent-path probe refuses with exit2/INVALID_ROOT. Evidence retained privately.
- Tool edit and process evaluation both reserved before effects and their
  actual receipts stored. Host-receipt helper is the updated checkout version,
  not falsely attributed to the older installed snapshot.
- Final measured result reported to the same ChatGPT thread; delivery receipt
  pending. This completes the bounded corrected inventory evaluation, not the
  global acceptance gates, another user's installation or paired benchmark.

## Installation lifecycle implementation and evidence

- Installer records exact owned bytes in .cortex-install.json and bundles the
  lifecycle helper. Helpers disable bytecode writes to avoid mutating their
  owned installation during normal operation.
- Reviewed hash-bound plan supports archive uninstall or prepared replacement
  activation. Changed/unowned/symlink files and occupied destinations refuse.
  Archives remain outside the explicitly supplied discovery root; no deletion.
- Nine targeted tests PASS. Actual isolated Codex CLI prompt discovery sees
  the installed skill, then no longer sees it after archival; bytes retained.
  Private evidence pointer: .qa-live-v061/SUPERVISOR_LIFECYCLE.md.
- Two renames are not an atomic swap; phase-specific recovery is documented.
  Concurrent hostile writers, crash recovery, legacy ownership migration and
  live desktop reload are not proven by these tests. No global install changed.

## Explicit activation recovery

- Added --recover-activation for absent active target with intact archived and
  replacement copies. It requires a newly reviewed plan hash, rechecks bytes
  and never archives/deletes the old copy again.
- Twelve lifecycle/installation tests PASS. Cases include an injected second
  rename error, a real subprocess os._exit(17) immediately after first rename,
  successful recovery, reappearing active directory and changed candidate.
- New target/user file and altered candidates are preserved on refusal.
  This proves tested process-interruption handling, not power-loss fsync
  guarantees or hostile concurrent writer safety. No global installation changed.

## External task B preflight started

- Final inventory report reconciled on two native reads; user message
  [private receipt ID retained locally], assistant [private receipt ID retained locally].
- Sent a bounded task-B preflight request through installed journal, explicitly
  requiring missing brief/reference before code. Its delivery remains pending.
- Frozen synthetic brief and SVG visual source prepared in private QA files
  BOARD_BRIEF.md and BOARD_VISUAL.svg. Neither has been transmitted yet.
  Preflight incorrectly described them as already-local before creation; next
  message must clarify their actual creation/transmission sequence.
- Task B is a functional pilot, not a clean-context paired benchmark. Reference
  rendering, delivery, implementation and browser acceptance are still open.

## Task B context delivered; receipt incompatibility exposed

- Preflight reconciled. Synthetic PNG rendered and visually inspected; actual
  brief and PNG uploaded with native macOS picker in the same conversation.
  The injected image chooser stalled; its draft attachment was removed before
  a successful native upload. One actual user message, no duplicate resend.
- Assistant [private receipt ID retained locally] correctly identified Field Notes,
  title length 1–120, exact recovery requirements and visual layout/card titles.
  This proves requested context provision/read, not task implementation.
- The old installed journal rejects the actual two host observations: the
  observer returns one newline before the marker and appended attachment
  descriptions. Preserve its FAIL and unresolved send rather than stripping
  text or weakening the exact-hash contract for this run.
- Next implementation: explicit, tested attachment-observation handling, with
  raw observations retained and ambiguity rejection. No blind send retry.
- Fresh native journal/actions/install/lifecycle subset: 23 tests, 2.548 s,
  exit 0. No new full-suite, task-B or benchmark claim.

## Explicit rendered receipt correction

- Three regression tests first failed, then passed after implementation.
  Strict confirmation unchanged. New opt-in confirm_rendered validates narrow
  boundary/summary grammar and exact body hash, preserves raw receipt, and
  refuses changed body, count, tail, observation identity and invalid counts.
- Fresh 34-test native/action/install/lifecycle/recovery subset PASS, 2.979 s.
- Two fresh native reads reconciled the actual task-B message using the updated
  checkout helper against the existing pilot database, exit 0. Old installed
  snapshot is unchanged. Original strict FAIL is retained, not relabelled.
- Implementation request now sent once through the installed journal/native
  host in the same conversation. Pending marker
  [private receipt ID retained locally]; do not resend. Await actual code, then
  independently evaluate the initial board before the declared change request.
- Implementation request subsequently reconciled, actual user message
  [private receipt ID retained locally]. Brain response
  [private receipt ID retained locally] arrived but is truncated at the host's
  20,000-character limit. No partial source executed. Asked for unchanged
  app.js in bounded chunks; pending send [private receipt ID retained locally].
- Official installed-skill validator PASS. Full backend regression started;
  do not substitute the focused result for its still-running result.
- Full backend subsequently completed: 805 tests PASS, 173.931 seconds, exit 0.
  Existing unclosed SQLite ResourceWarning persists. No frontend or external
  task-B acceptance follows from this backend run.

## Task B first real browser lot

- Received complete JS retransmission and reconciled its request. Copied full
  HTML/CSS/JS from brain fences into an isolated synthetic pilot, without repairs.
- Independent browser evaluator: 10/10 scenarios PASS, exit 0. Covers initial
  CRUD/persistence, combined filters, text safety, keyboard creation/cancel,
  corruption recovery and failure handling, 375/768/1440 rendering.
- Three screenshots visually inspected. Actual files and hashes recorded in
  private .qa-live-v061/board-pilot/PILOT.md and evidence.json.
- README requested separately; pending marker [private receipt ID retained locally].
  Remaining boundary/keyboard coverage and predeclared priority migration next.
  No paired benchmark or full task-B completion claim.

## Task B initial boundaries verified; change requested

- README received and request reconciled. Four extra browser scenarios plus
  original lot: 14/14 PASS, same application hashes. Keyboard edit/delete/cancel,
  exact input limits, move buttons and read-storage failure verified.
- Evidence retained separately; no rewrite of the earlier evaluator/results.
  Boundary run used direct process execution, not a pre-reserved native action.
- Predeclared priority/filter migration requested in the same brain discussion;
  pending marker [private receipt ID retained locally]. Await actual patch, preserve
  original application and evaluate migration success and storage-write failure.
- Migration reply subsequently received in full, [private receipt ID retained locally],
  10,890-character patch retained privately. Request reconciled; patch not yet
  applied or validated. Apply to a separate candidate to preserve initial proof.

## Task B priority candidate evaluated

- Applied brain migration diff to separate board-priority candidate; originals
  preserved and hashes rechecked. Syntax PASS, initial migration suite 18/18PASS.
- Stronger successful-retry UI check: 17PASS/1FAIL. Data migration succeeded,
  but stale failure banner remained alongside Saved on this device. Screenshot
  and both scopes of evidence preserved. README has an obsolete final sentence.
- Reported exact failure to same brain conversation; pending correction request
  [private receipt ID retained locally]. No silent operator fix or completion claim.
- Private evidence: .qa-live-v061/board-priority/PILOT.md. This is a functional
  external pilot, not paired comparison or complete journal-mediated execution.

## Recovery correction evaluated

- Brain scoped migration-error clearing, correction reconciled and applied.
  Its changed clearError signature broke the direct click listener. Valid
  browser probe confirmed Dismiss failed; initial invalid behind-modal probe
  separately retained. Operator added only () => clearError() callback wrapper.
- Final serialized browser run 18/18PASS; intermediate files and failures kept.
  Concurrent runs also showed keyboard-delete intermittency, still UNCLEAR;
  do not attribute its later pass to the unrelated callback repair.
- Report sent to brain, marker [private receipt ID retained locally] pending.
  Remaining: isolate focus intermittency, then comparative executor trials and
  still-open native lifecycle/supervision/release gates. No release claim.

## Task B focus cause reproduced and repaired

- Controlled slow-frame browser test proves deferred rAF steals focus after Tab.
  Real FAIL retained; direct focus at four sites gives PASS on identical test.
- Operator applied the scoped change; subsequent brain response
  [private receipt ID retained locally] agrees exactly, request reconciled.
- Fresh18/18 browser regression PASS and screenshots inspected. Task B functional
  initial+change flows now validated at tested scope, with operator contributions.
- Freebuff local version0.0.167/help verified. No documented batch flag in help;
  subsequent UI selects GLM 5.3 Flash/max/unlimited; synthetic execution PASS,
  unchanged output passes three independent checks. Application label only,
  not provider attestation. No paired trial or full Cortex integration claim.
  Native S2–S6 gaps and S8/S9 release requirements remain open.
