# Cortex Bridge 0.6.2 simple harness verification

Date: 2026-09-11
Branch: `codex/v061-atelier`
Base sealed commit: `821fddd`

This is a local candidate verification for the 0.6.2 workstream. It does not
claim a published 0.6.2 release or native desktop attachment support.

| Gate | Result | Evidence |
| --- | --- | --- |
| Context approval identity | PASS | `request_id` and `item_id` are validated and must match the proposed item. |
| Duplicate approval | PASS | The same conversation/request/item tuple returns the existing run without a second transport call. |
| Workspace confinement | PASS | Absolute paths, traversal and missing workspaces are rejected. |
| Link/screenshot bounds | PASS | Only `http(s)` links and the two supported screenshot targets are accepted. |
| Backend focused tests | PASS | Context approval, supervisor context and regression tests pass. |
| Frontend focused tests | PASS | 29 tests for the context parser and workspace approval card. |
| Full Python suite | PASS | 746 tests passed. |
| Full frontend unit suite | PASS | 214 tests passed. |
| Frontend runtime/privacy suite | PASS | 33 tests passed through the installed `tsx` binary. |
| TypeScript | PASS | `frontend/node_modules/.bin/tsc --noEmit`. |
| Lint | PASS | `frontend/node_modules/.bin/oxlint --deny-warnings .`. |
| Lint contract fixtures | PASS | 6/6 rule families rejected their intentionally invalid fixtures. |
| Production build | PASS | `frontend/node_modules/.bin/next build`. |
| Secret scan | PASS | `gitleaks detect --no-banner` found no leaks. |
| Diff hygiene | PASS | `git diff --check` passed. |
| Browser and accessibility suite | PASS | 26 Playwright tests passed, 1 optional guide test skipped; the responsive collision suite passes at 375/768/1024/1440. |
| Links and public privacy | PASS | 133 offline link checks; privacy scan covered 428 files and 92 images with no findings. |
| Visual context card | PASS | Synthetic screenshots in `docs/screenshots/v0.6.2/context-request.png` and `docs/screenshots/v0.6.2/context-request-states.png`. |

| ChatGPT Pro static review | READY | 13 production files inspected, including `orchestration/runner.py`; no tests were executed by ChatGPT. |

Known limits:

- The desktop ChatGPT application's native attachment capability is not exposed
  by this repository, so no real desktop file transfer is claimed.
- Automated browser evidence uses synthetic fixtures; intentional fixture
  `503` responses are not product failures.
- The existing SQLite `ResourceWarning` remains observed.
- The repository requires npm `11.18.0`, while this macOS environment has
  npm `11.11.1`; direct installed binaries were used without changing the
  dependency tree.
- The working tree contains the 0.6.2 workstream and is not yet committed or
  pushed.
