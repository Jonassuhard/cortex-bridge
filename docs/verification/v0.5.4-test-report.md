# Cortex Bridge v0.5.4 verification report

**Verification date:** 2026-09-06
**Source commit under test:** `e4d0bc5fcf6e751bcea9608d179872ef8ff69472`
**Scope:** deterministic local checks, browser fixtures, fail-closed storage
diagnostics, and release-evidence preparation.

## Result

The branch is not a release candidate. Most deterministic product and UI gates
pass, but the evidence remains `UNCLEAR` because the real installed-generation
launcher is absent, provider/browser effects were not executed, and the Python
3.12 full suite recorded one observation-triggered descendant-cleanup error.

## Executed evidence

| Area | Result | Evidence |
| --- | --- | --- |
| Python backend, Python 3.14 | PASS | 998 tests completed without unittest failure |
| Python backend, Python 3.12 | UNCLEAR | 998 tests, 1 error in `test_outer_timeout_and_observation_remove_separate_descendant_groups` during the full run; isolated rerun PASS |
| Focused S3/runtime contract | PASS | 50/50 on Python 3.14 and 50/50 on Python 3.12 |
| Launcher and doctor | PASS | 17/17 regression tests |
| Chrome extension | PASS | 130 tests, 0 failures |
| Frontend unit/coverage | PASS | 155 tests, 21 files; coverage is recorded in the focused gate |
| Browser E2E | PASS | 12 passed, 1 intentionally skipped |
| Accessibility | PASS | 4 responsive viewports |
| Runtime verification | PASS | `ok: true`, diagnostic-only fallback confirmed |
| Privacy scan | PASS | 392 files and 43 images scanned |
| Dependency audit | PASS | 0 high-severity vulnerabilities |
| Link checker | PASS | 124 links checked, 55 external checks |
| Swift compilation | PASS | Both native sources compile; one existing deprecation warning |

## Performance observations

The deterministic fixture measured 148.2 ms cached usability. Ten fixture
conversation switches measured a 63.6 ms p95 and 63.6 ms maximum. These are
local fixture observations, not measurements of a signed-in ChatGPT session.

## Storage/startup diagnosis

The configured encrypted volume is not mounted in the current macOS session.
The guard correctly fails closed with `STORAGE_VOLUME_MISSING`; it does not
mount or unlock disks. The source launcher now preserves that status and the
doctor output exposes it as a required `external_storage` check.

## Corrective work completed

- Added descriptor-relative selector/generation/interpreter validation before
  native broker attestation.
- Added a selector-digest regression test.
- Added storage-aware doctor output and actionable launcher diagnostics.
- Preserved the exact storage failure code through the `go` command.
- Kept user-owned generated UI, primer, and unrelated test edits out of the
  release commit.

## Deliberately unclaimed evidence

- No real ChatGPT conversation, file upload, screenshot upload, or provider
  interaction was executed in this verification run.
- No clean macOS account or VM lifecycle was executed.
- No real Keychain, `hdiutil`, `diskutil`, external vault, or browser-profile
  mutation was executed.
- No installed generation, fixed server entrypoint, or managed runtime lease is
  published and exercised.
- No release, tag, push, merge, or public publication is authorized by this
  report.
