# Cortex Bridge v0.5.3 test report

Date: 2026-08-26. Public verdict: `OPT_IN_TECHNICAL_PREVIEW`.

This report contains aggregate, synthetic evidence only. It does not contain a
ChatGPT account, conversation title, message, cookie, private path or raw live
screenshot. The audited source commit is recorded separately in
`docs/verification/v0.5.3.json` after the source tree is committed and clean.

## Final automated gates

| Gate | Result |
|---|---:|
| Python backend | 625 passed, 0 failed |
| Chrome extension | 126 passed, 0 failed |
| Frontend unit | 145 passed, 0 failed |
| Frontend runtime/privacy | 35 passed, 0 failed |
| Browser E2E | 12 passed, 0 failed, 1 intentional guide skip |
| Accessibility | 4 passed, 0 failed at 375, 768 and 1440 px |
| Crash and recovery points | 6 passed, 0 failed |
| Chrome recovery/anti-replay | 6 passed, 0 failed |
| Cold dual-writer fixture runs | 10 passed, 0 crossovers |

The dual-writer fixture preserved the refused third conversation's draft and
staged file in every run. Cached usability measured 231.1 ms; switch p95 and
maximum both measured 141.7 ms. The fixture browser reported zero unexpected
page, console or hydration errors.

## Build, package and lifecycle

- Two consecutive normalized production builds contained 28 files and shared
  aggregate SHA-256
  `a401609dd88bc4fc2562ffd4563c07854105f3d4c8324198ee5a712ec180666a`.
- A clean Git archive contained the Freebuff guide, generated static chunk,
  Chrome extension and native Swift helper.
- The archive passed 126 extension tests and six version/package tests.
- A PEP 517 wheel contained `transport/macos_ax_send.swift`.
- A temporary isolated `CORTEX_HOME` passed immutable dry-run approval,
  install, Doctor, start, owned status, loopback API status, stop, idempotent
  reinstall and manifest-based uninstall without `sudo`.
- Foreign sentinel files survived uninstall and no listener remained.

## Security and public-tree gates

- Privacy scan: 326 files, including 42 images, passed metadata, OCR, private
  marker, path and URL checks.
- Link scan: 121 links passed, including 56 external checks.
- Gitleaks: 240 commits scanned, no leak found.
- npm audit and `pip-audit`: no known vulnerabilities.
- ShellCheck, Python compilation, runtime surface verification and
  `git diff --check`: passed.

## Explicit crash/recovery mapping

The six historically mapped tests cover: stored restart recovery, surfaced API
recovery, browser startup failure eviction, database-write compensation,
runtime-construction compensation, and cancellation while browser/background
work is quiescing. All six passed in one explicit targeted run after the full
backend suite. Because that historical set does not count every possible send
or action, six additional Chrome tests proved closed-tab/content-script recovery,
retry only before proven delivery, and no replay after uncertain delivery.

## Disposable self-diagnostic

A detached disposable worktree completed exact-plan install, replay refusal,
Doctor, start, owned status, `/api/status`, `/api/tasks`, stop, idempotent
reinstall and uninstall. The final API reported version 0.5.3, the port was
released, and the worktree was removed without merge.

Two earlier evidence-wrapper attempts are retained in the attempt count: the
product lifecycle reached its expected states, but the wrapper failed while
decoding an empty summary value. They are not counted as passing runs. The
final run used stage-by-stage assertions and exited successfully.

## Boundaries still open

- No genuinely clean macOS account or VM has repeated the lifecycle.
- The live third-writer refusal has not preserved a staged file as public
  evidence.
- A real cold/warm ChatGPT conversation switch is not freshly timed.
- Live tab-close/reload recovery and a final authenticated console scan remain
  unrecorded.
- Provider terms still block a compliant consumer-site automation release.

These gaps prevent a `READY` verdict. They do not invalidate the local fixture,
security, packaging or isolated lifecycle evidence above.
